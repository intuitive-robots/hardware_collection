# configs/config.yaml
data_dir: ./records               
robot_name: franka               
state_topic: robot_state 
capture_interval: 0.03           

camera_streams:
  zed_left:
    topic: zed_left    
    recv_timeout_ms: 1000
  zed_right:
    topic: zed_right    
    recv_timeout_ms: 1000
  zed_wrist:
    topic: zed_wrist
    recv_timeout_ms: 1000            
